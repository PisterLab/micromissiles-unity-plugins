load("@rules_pkg//pkg:tar.bzl", "pkg_tar")

package(default_visibility = ["//visibility:public"])

cc_shared_library(
    name = "example",
    deps = ["//example"],
)

pkg_tar(
    name = "plugins",
    srcs = [
        ":example",
    ],
    extension = ".tar.gz",
)
